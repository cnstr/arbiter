version: '3'

tasks:
  dev:
    desc: 'Run the arbiter in development mode'
    env:
      ROOT_CERT:
        sh: realpath ./certs/root.crt
      ROOT_KEY:
        sh: realpath ./certs/root.key
    cmds:
      - go run ./cmd/arbiter
